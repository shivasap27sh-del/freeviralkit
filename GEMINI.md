# FreeViralKit Agent Instructions & Superpower Protocols

## 🔱 Always-On Core Mindset Rules (Enforced on EVERY Task)
1. **`ponytail` (Minimalist Senior Builder):** Standard library first, native platform features, YAGNI, single lines, and shortest diff. Never over-engineer or add unrequested abstractions.
2. **`root-cause-detective` (Forensic Debugger):** ZERO code edits during investigation. Formulate 2–3 hypotheses, gather binary proof, and apply a surgical 1-point fix at the root cause. Never blind-patch or stack failed edits.
3. **`simplify` (Anti-Entropy Cleaner):** Max 1 level of nesting with guard clauses & early returns, derived state over `useEffect` sync, single source of truth for utilities, and aim for net-negative line diffs.
4. **`tdd-iron-loop` (Verification Oracle):** Never declare a bug fix, parser, or logic change complete without automated test proof. Follow Red-Green-Refactor to guarantee zero regressions.
5. **`impossible-states` (Type Invariant Shield):** Eliminate boolean soup with discriminated unions (`{ status: 'idle' } | { status: 'loading' } | { status: 'success'; data } | { status: 'error'; message }`), exhaustive matching with `never`, and zero state desyncs.
6. **`design-taste-frontend` (Design Taste & Anti-Slop):** Ban generic 3-card clones, predictable centered hero traps, and robotic AI copy. Enforce layout diversity (4+ layout families per page), tactile spring motion, obsidian glass depth, and pre-flight contrast audits.

## 🚀 FreeViralKit Project Rules
- **Repository Remote:** Commit and push to `https://github.com/shivasap27sh-del/freeviralkit`.
- **Deployment Freeze:** AdSense approval is pending. Do NOT deploy or push to Git/Vercel without explicit user request. Remind Shiva to sync `.env.local` variables into the Vercel Dashboard whenever deploying.
- **YouTube API & UX:** Adhere strictly to `youtube-studio-ux` and `api-rate-limit-resilience` for YouTube quota efficiency and UI character gauges.
- **Security & Safety:** Validate all Server Actions with Zod schemas, prevent SSRF/XSS, and maintain AdSense compliance.
