// Layout wrapper for youtube-shorts-idea-generator route.
// Metadata is defined in page.tsx (which overrides layout-level metadata in Next.js).

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
